apiVersion: v1
kind: Service
metadata:
  name: devsecops-backend
  namespace: devsecops
spec:
  selector:
    app: devsecops-backend
  type: NodePort
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30080
