apiVersion: v1
kind: Service
metadata:
  name: devsecops-backend
  namespace: devsecops
  labels:
    app: devsecops-backend
spec:
  type: NodePort
  selector:
    app: devsecops-backend
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30080